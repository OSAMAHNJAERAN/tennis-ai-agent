# Copy to a local environment file outside version control, or set process variables.
# Full URL of an OpenAI-compatible chat-completions endpoint (not a base URL).
ASTRA_GPT_ENDPOINT=
ASTRA_GPT_MODEL=
ASTRA_GPT_API_KEY=
