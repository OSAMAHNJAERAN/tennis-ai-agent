# Model progress

2026-09-14 pending diagnostic: a frozen 44-label reserved-training response study is prepared (23 proposal misses, 12 correct controls, 9 absence controls). Source-path inventory and syntax checks pass. Project-runtime tests were rejected before launch by automatic approval review because account usage was exhausted; no GPU run or accuracy result exists. See docs/experiments/WASB_PROPOSAL_RESPONSE_STATUS.md. This is preparation only and does not change model selection or readiness.

2026-09-14: opt-in original-view court recovery passes five controlled camera-cut cases, independent artifact verification and 35 focused tests. Both sustained returns resume at frame 96 (0.10 seconds after return); healthy matrices remain exact and unrelated/brief views remain withheld. A separate 150-frame real-model CUDA pipeline check reproduces 114 valid frames, preserves missing ground coordinates/speeds across the gap, and fully decodes the annotated video. Runtime including model setup is 48.8064 seconds. Existing configs/models remain unchanged; the new opt-in recipe is wasb_returning_view_validation.yaml. This does not establish natural-cut, identity, ball or physical-speed accuracy. Production readiness remains NO. See docs/experiments/COURT_RETURNING_VIEW_RESULTS.md.

2026-09-14: the controlled FPS-aligned head experiment completes and fails its frozen gate. At fixed .20 on 600 internal-selection labels, aligned and adjacent heads both score P91.1263% / R93.8489% / F1 92.4675%, while aligned correct-proposal coverage falls from 546 to 545 of 569 visible labels. All sample/view/output-slot choices match the seeded reference, all non-head tensors remain exact, and independent scores/provenance verify. The sole recovery over original weights was already achieved by the adjacent head and is visually reviewed. Twenty-two focused tests pass. No external run or runtime promotion follows; the verified aligned cache remains reusable. See `docs/experiments/WASB_SPACED_HEAD_PILOT05_RESULTS.md` and `WASB_SPACED_TRAINING_DATA_RESULTS.md`.

2026-09-14: head-only WASB adaptation completes training and both verified selection/development comparisons. On 900 development labels, selected-0.35 precision/recall rises from 95.4272%/95.6574% to 95.5475%/95.7780%, entirely from one blurred-streak localization crossing the tolerance boundary. The frozen raw gate passes; proposal coverage stays 798/829 and only eight of eighteen clips meet both targets. A separate candidate-preserving feasibility check caps recall below the current filtered pipeline (798 versus 803 possible/current correct balls), so further continuous compute is held pending better proposals. No runtime promotion or readiness change follows. All 900 reused points/candidates and independent scores verify, the sole changed outcome is visually reviewed, and sixteen focused tests pass. See `docs/experiments/WASB_HEAD_EXTERNAL_RESULTS.md` and `WASB_HEAD_ADAPTATION_RESULTS.md`.

2026-09-14 training preparation: acquired the next fixed twenty publisher-training clips and verified the full sixty-clip corpus (2,999 labels; 48 train / 12 internal-selection clips). All new 6,751 frames decode, all source hashes and split/count checks pass, and all 35 preselected visual examples are inspected. Eight new data checks pass. This adds training evidence and identifies contact/absence review limits; no model training or accuracy improvement is claimed. See `docs/experiments/BALL_TRAINING_EXPANSION60_RESULTS.md`.

This index starts with the resumed 2026-09-13 checkpoint. Earlier work remains in `CURRENT_SYSTEM_AUDIT.md`, `PRODUCTION_READINESS_REPORT.md`, and the linked experiment reports; it has not been rerun or replaced.

| Comparison | Previous | Candidate | Measured change | Decision |
| --- | --- | --- | --- | --- |
| FPS-aligned versus adjacent-context head, fixed .20, 600 reserved labels | Adjacent head: P91.1263 / R93.8489 / F1 92.4675%; proposal coverage 546/569 | Aligned head: identical outcomes; coverage 545/569 | No added correct detections; one near-threshold correct proposal lost | Frozen gate fails; reject external promotion compute; preserve reusable aligned cache |
| WASB head-only epoch 01, 900 reused development labels / 18 clips | Original 0.35: P95.4272 / R95.6574 / F1 95.5422% | Head 0.35: P95.5475 / R95.7780 / F1 95.6627% | One boundary localization corrected; proposal coverage unchanged at 798/829 | Raw gate passes; candidate-only recall ceiling below current filtered 803 TP, so hold continuous promotion work and improve proposals |
| WASB training-only threshold selection, 400 labels / 8 reserved training matches | Original weights, selected threshold 0.35: P95.4178 / R93.6508 / F1 94.5260% | Spatial epoch 03, selected threshold 0.50: P95.6522 / R93.1217 / F1 94.3700% | Two fewer true balls, one fewer false detection; F1 -0.1561 percentage points | Retain rejection; no runtime model/threshold promotion |
| Original WASB threshold transfer, 900 reused development labels / 18 clips, sparse raw output | Threshold 0.20: P93.4884 / R96.9843 / F1 95.2043% | Training-selected 0.35: P95.4272 / R95.6574 / F1 95.5422% | 18 fewer false detections, 11 fewer true balls; individually passing clips 10 to 8 | Pooled targets pass, frozen no-recall-loss gate fails; no runtime promotion |

The second row is newly executed GPU evidence: all 900 control points reproduce exactly, independent scoring passes, five visual examples are reviewed, and seventeen focused tests pass. The shared two-threshold sparse run totals 814.43 seconds across clips, excluding setup; it is not end-to-end FPS. See `docs/experiments/WASB_SELECTED_THRESHOLD_RESULTS.md` for the preserved tradeoff and complete per-clip results.

Evidence: `docs/experiments/WASB_TRAINING_THRESHOLD_RESULTS.md`. All ten pooled scores replay exactly, sixteen saved inference-path checks are verified, five examples are visually inspected, and eleven focused tests pass. Saved sparse timings are 310.37 versus 331.96 seconds; they do not establish end-to-end speed changes. No training or inference was repeated for this completion review.

The current spaced-ball pipeline's previously measured development result remains P94.9173% / R96.8637% across 900 labels, as documented in `docs/experiments/BALL_SPACED_ERROR_AUDIT_RESULTS.md`. This uses different data and continuous filters from the internal comparison above and must not be presented as a paired before/after result.

Next work: improve proposal coverage and training-negative quality, preserve match-based model selection, and qualify on independent footage. Court/player camera generalization and true physical ball-speed validation remain unresolved. Production readiness: **NO**.
