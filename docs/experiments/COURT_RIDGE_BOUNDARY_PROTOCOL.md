# Frozen combination of ridge evidence and boundary preservation

Combine the already tested `no_white` proposal mode with the already tested boundary-preserving refinement. Make no new threshold, loss, assignment, constraint or detector change. Evaluate all 64 saved cases from the completed no-white report, using its exact selected segments and initial projected points. Verify its report/code/source hashes, and replay the original unconstrained optimizer on those saved segments to reproduce the no-white coordinates and acceptance before running the constrained optimizer. Invalid geometric initializations remain withheld.

This combination is selected using previous development results and is explicitly development tuning. It is not a new independent test. Retain all labels, canonical identities, out-of-image exclusions and inclusive seven-native-pixel scoring. Report the 32 selection and eight external-source-check groups separately; compare the combination against initial projection, original refinement, no-white alone and boundary preservation alone. Include every gain and loss, available coverage, acceptance change and retained initial-point regression. The 24 pilot cases have no keypoint ground truth and include repeated model/view evaluations.

Reusing saved image segments avoids rerunning the detector or photometric analysis. No label may enter the refinement call or acceptance. Hash every referenced source and rendered artifact. Independently replay labeled counts and boundary invariants after completion. Render every changed case relative to no-white alone and inspect all changes and remaining initial-point losses. Keep previous reports and algorithms unchanged. No runtime promotion or physical-accuracy claim follows solely from improved development agreement.
