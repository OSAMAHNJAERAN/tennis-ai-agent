# Court refinement raw-label check

Fixed before running the current ResNet checkpoint on these images. Use all eight `external_source_check` images in the completed `court_heatmap_pilot` manifest. These source IDs are absent from the publisher heatmap training split; independence from the ResNet checkpoint's training is unknown. Publisher labels already have known errors. Treat the outcome as agreement with unchanged annotations, not independent court accuracy.

Run the existing native-resolution `CourtKeypointDetector` with `models/keypoints_model.pth`, unchanged preprocessing and coordinate convention. Compare three stages: raw fourteen model landmarks; canonical landmarks projected by the existing geometric calibration if accepted; and the same projected geometry after the frozen local line refinement. Invalid geometric initialization emits no calibrated landmarks at either latter stage. A rejected local correction retains its original projected geometry. No labels enter detection, calibration, segment proposal, refinement or acceptance.

Use exactly the existing refinement function and line-proposal thresholds. At width960, retain segments with supported fraction at least0.5. Map accepted/refused refined points back to native1280x720 before scoring. Match by canonical keypoint ID, with seven native pixels as the fixed threshold. Wrong locations count FP+FN, missing predictions FN. Exclude only out-of-image labels; report all supplied/eligible counts. Save native distances and per-image results, including regressions, without threshold changes or label removal.

Render all eight images with publisher labels, raw predictions, projected initialization and final predictions. Review label disagreements on the original image context; do not silently correct annotations. Report geometric-initialization acceptance separately from refinement acceptance and raw model coverage. A small or empty usable sample is a limitation, not evidence that the method generalizes.
