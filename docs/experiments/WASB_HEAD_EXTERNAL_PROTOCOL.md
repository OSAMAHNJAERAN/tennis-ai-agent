# Frozen external comparison of the head-only WASB pilot

Frozen 2026-09-14 after independent internal-selection verification and before adapted external inference. Pilot04 updates only the final prediction head for one epoch. Both original and adapted model independently select threshold 0.35 on the twelve reserved publisher-training clips. Internal F1 increases only 0.023724 percentage points with two recovered balls and two added absent false detections. This satisfies the existing strictly-higher-F1 gate but does not establish a useful generalization gain.

Compare the frozen original checkpoint and pilot04 epoch_01 checkpoint at their selected 0.35 thresholds on all 900 unchanged labels from the existing eighteen development clips. Use the unchanged five spatial views, FPS-adjusted real overlapping temporal windows, heatmap averaging, candidate decoding and confidence selection. Do not search thresholds, train another epoch, alter labels or tune filters during this comparison. The dataset is external to the current train/selection split but repeatedly reused for development, not independent qualification.

Reuse original 0.35 predictions from the completed, independently reviewed wasb_selected_threshold01 report, checking its report/review hashes, inference-code hashes, model hash, every dataset/video/label hash, frame geometry, label identity and window alignment. Preserve original 0.20 results as contextual comparison only. Verify match identifiers are disjoint from all sixty publisher-training clips; source-broadcast independence and original-checkpoint exposure remain unknown. Infer only the adapted 0.35 pair, saving every selected point and all candidates. For the first label in each clip, require exact agreement with the internal-selection inference helper at 0.35, including all candidates and source windows.

Report pooled and per-clip precision, recall, F1, TP/FP/FN/TN, absence errors, proposal coverage, and paired outcome changes. Use the existing 4-pixel tolerance at 512x288 reference resolution. Independently recompute metrics and inspect the first two outcomes per transition category in clip/frame order, including abstention-to-wrong-location changes. No statistical significance is assumed from sparse dependent frames.

Advance to a continuous filtered comparison only if adapted pooled F1 is strictly higher, precision and recall are each no lower than original 0.35, and the number of individually passing clips (precision and recall each at least 0.95) does not decrease. Otherwise reject this model/threshold pair for promotion and preserve the measured tradeoff. This gate is frozen before adapted external results. Passing still requires a paired comparison against the current filtered 0.20 runtime and independent qualification before any runtime replacement.

Expected cost: approximately fifteen minutes on the existing RTX 4050, with no training or downloads. Persist each completed clip atomically. A resume must verify the entire frozen provenance and must not overwrite a completed result. Stop on nonfinite output or provenance mismatch. Sparse timings are not end-to-end FPS.
