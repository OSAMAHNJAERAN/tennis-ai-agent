# Frozen remaining court pilot check

Run all 125 existing train-partition images from the completed court_heatmap_pilot manifest in manifest order. These images were used in the earlier heatmap training pilot and occur in publisher training data. They have not formed the 40-image refinement selection comparison, but are explicitly reused development data, not independent qualification. Independence from ResNet training is unknown. Keep all publisher labels, including errors.

Freeze the current ResNet detector, preprocessing, native calibration and two compared refinement methods: original line evidence plus original optimizer, and no_white line evidence plus boundary-preserving optimizer. Use exact existing implementations, no threshold or algorithm changes. Score raw, calibrated projection, original refinement and combined refinement by identity and inclusive seven-native-pixel distance. Wrong locations count FP plus FN; missing predictions FN; exclude only out-of-image labels. Preserve every image and report coverage separately. Labels enter after all model/geometry/acceptance decisions.

Verify dataset, checkpoint and the previous combined experiment code hashes before inference. Save all native predictions, distances, evidence, source/code/protocol hashes and all four-stage image panels. Independently replay scoring and labels after completion. Count all gains and losses against projection and original refinement. Inspect every image that loses any initially correct match and every image with a net regression versus original refinement. Inspect representative gains with their selection rule recorded; do not imply all 125 panels were visually inspected unless that actually occurs. No threshold retuning or label correction during this run. No runtime promotion based only on this broader but non-independent check.
