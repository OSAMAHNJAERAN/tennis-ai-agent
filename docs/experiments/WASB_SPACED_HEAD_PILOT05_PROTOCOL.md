# Controlled FPS-aligned head adaptation

Frozen 2026-09-14 before training pilot05. Pilot04's head-only transfer changes only one external localization at its training-selected .35 threshold, whose proposal ceiling is below the current .20 pipeline's recall. Test the specific remaining training/inference temporal mismatch at the actual deployed .20 operating point, without selecting another threshold on external data.

Use the completed, independently verified sixty-clip FPS-aligned cache after all three fixed context examples are visually inspected. Exactly 2,399 source labels train and 600 labels from twelve reserved publisher training matches provide internal evaluation. Preserve pilot04 sample order, view sampling, explicit targets, output-slot distribution, augmentation, optimizer, seed, parameter scope and epoch count. Relative to pilot04, only the five temporal input paths change for 699 high-FPS training labels; all stride-one paths are identical. No new labels, data, augmentation or loss are introduced. Selection frames already used FPS-adjusted context in pilot04, so they are unchanged.

Initialize from the untouched official WASB tennis checkpoint. Train only final_layers.* for one epoch, seed7, batch1, AdamW1e-5, weight decay1e-4, AMP, gradient clipping1 and frozen batch normalization. Use the existing three-update smoke and restore all model/optimizer/scaler/random state before training. Require all non-head tensors exact and actual sampled visible/absent counts equal pilot04 (1,378/1,021). Record a seeded reference-loader schedule on pilot04's sample list before training, then require the actual training-label order, sampled views and output slots to match it exactly; this is input-schedule verification, not repeated model training. No CUDA bitwise reproducibility claim is made.

Infer all 600 internal-selection labels for the new checkpoint using unchanged five-view FPS-aligned overlapping windows and threshold .20. Record all candidates and selected coordinates, per-clip scores, correct-proposal coverage and raw correct/absent outcomes. Reuse verified pilot04 original and adjacent-head .20 predictions after checking source/code/model hashes and every label/window. Do not reselect thresholds or epochs. First label per clip must agree exactly with the existing multi-threshold helper's .20 path.

Internal advance gate: candidate pooled F1 strictly above original .20, precision and recall each at least original .20, and correct-proposal coverage at least original .20. Also compare against adjacent-head pilot04 at .20: candidate F1 and coverage each no lower, with at least one strictly higher. Report all per-clip and absence regressions regardless of the gate. If it fails, reject this spacing-only head candidate for further compute. If it passes, freeze a later external .20 comparison against the current original .20 recipe, preserving all settings and proposal evidence. Internal success alone never changes runtime defaults.

Persist the trained checkpoint and each completed evaluation clip. Protect completed outputs. If interrupted after checkpoint creation, a separate evaluation-resume path must verify provenance and reuse the saved checkpoint; do not retrain automatically. Expected GPU cost: approximately four minutes training and eight minutes sparse internal evaluation, with cached original and pilot04 results. No paid compute or downloads. Sparse runtime is not end-to-end FPS; all evidence remains development rather than independent qualification.
