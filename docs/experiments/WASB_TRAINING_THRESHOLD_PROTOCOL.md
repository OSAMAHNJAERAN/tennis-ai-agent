# Training-split operating-point check

Freeze this protocol before inference. Spatial pilot 03 selected epoch three using internal focal loss, then failed external precision at the untouched model's .20 heatmap threshold. That result does not separate score calibration from localization/discrimination. Compare the untouched official checkpoint and that already-selected epoch-three checkpoint using their eight reserved publisher training match IDs (400 labels). Do not select a different epoch here. No new training or validation-directed threshold search occurs.

Use the original training manifests and the same every-fifth-match split, verifying all sources and that no selected match is in publisher validation. Preserve original sparse active-ball coordinates and absence semantics. The original model's historical training exposure and source-broadcast independence are unknown; these are internal selection results, not qualification.

For both checkpoints, use full view plus four 60% corner crops, native FPS spacing max(1,floor(fps/30+.5)), all real three-frame windows aligned to each target, and unchanged peak-confidence ranking with four-reference-pixel duplicate suppression. Decode the same averaged heatmaps at each threshold in [.10, .20, .35, .50, .70]. Thresholding changes connected components and their centers, so do not approximate it by filtering a cached .20 candidate list. Save every candidate and selected point per threshold. Verify the .20 path against the existing inference function on the first labeled frame in every clip/model combination.

Independently score all 400 labels for each model/threshold at the unchanged inclusive four-pixel/512x288 tolerance. Wrong visible localization is FP plus FN. Select each model's threshold by highest pooled per-frame F1, breaking ties by precision, recall, then proximity to .20 and lower threshold. Freeze both resulting thresholds before external evaluation. Include per-clip results and explicit absence counts, not just pooled F1 or pixel loss.

If the adapted model loses internally even after this training-only selection, retain its rejection and report the calibration hypothesis unsupported on this evidence. If it wins internally, run both frozen model/threshold pairs on the complete eighteen-clip, 900-label development benchmark using identical sparse inference before considering any continuous filtering comparison. A sparse gain cannot establish full-pipeline accuracy, and no untested threshold enters the runtime default.

Use fresh output files, preserve checkpoint/source/protocol/code hashes, save each completed clip/model result, and expose completion state explicitly. The benchmark performs inference only with the existing restricted PyTorch loader and never loads either blocked RacketVision checkpoint.
