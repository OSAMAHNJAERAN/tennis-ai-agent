# Frozen boundary-preserving court refinement comparison

This is one development revision motivated by four lost landmark matches in two previously evaluated selection images. It is not independent qualification. Freeze the implementation and this protocol before running the broader comparison. Do not tune thresholds or labels based on the comparison.

Retain all original detector predictions, geometric initialization, segment proposal, local assignment, optimizer loss and acceptance thresholds. Add one change: if an outer boundary line (IDs 0, 1, 2, 3) is absent from compatible matches at any iteration, constrain its original infinite line for the rest of that refinement. Homography endpoint-on-line constraints are linear in the eight transform parameters. Optimize only in their nullspace, computed by SVD with rank tolerance 1e-10. A constrained line may retain along-line endpoint motion; this is not a promise of preserving every point or fixing bad initialization. Missing evidence must never become an invented observation. Reject unchanged if no transform freedom remains.

Evaluate all 32 selection images, all eight external-source-check images, and all 24 original refinement pilot cases. These are 64 reused case/model evaluations, not 64 independent views. Copy predictions from immutable reports; verify source images/videos, report and code hashes. Re-run the original refinement and verify acceptance, points and scoring against saved evidence before comparing the new one. Preserve invalid calibration cases as withheld; no new model inference or label-dependent selection.

Compare all labeled cases with unchanged canonical identities and inclusive seven native pixel tolerance, including every loss. Report selection and external-source groups separately, eligible labels, prediction coverage and projected/original/new scores. For all cases, report correction decisions and projected-line support. Save old/new/projected coordinates and rendered panels. Inspect every changed accepted result and any new label regression. Previously invalid initializations and unavailable ground truth must not count as successful corrections.

The two diagnosis cases and the minimal seven-segment regression fixture are development inputs. Their passing replay does not establish generalization. The 33 focused tests passed before this comparison, including exact projective boundary invariants and correction ability with observed and missing boundaries. Three initial synthetic test inputs exceeded the pre-existing 20-pixel matching range; they were reduced to within-range displacements without changing any algorithm threshold. Keep that distinction separate from failures on actual images.
