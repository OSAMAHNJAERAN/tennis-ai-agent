# FPS-aligned temporal training cache

Frozen 2026-09-14 before cache materialization. The completed pilot04 cache supplies adjacent-frame inputs even though deployment uses native-FPS-adjusted temporal spacing. Of 2,999 labels in the fixed sixty-clip corpus, 699 training and 150 internal-selection labels have stride two at 60 FPS. Align their input context to the existing deployment rule without changing source targets, crop geometry or split membership. This is data preparation, not a claim of model improvement.

Use exactly the verified pilot04 training sample manifest and the completed sixty-clip source audit. Require all source manifest/video/CSV hashes unchanged and every sample's clip/frame/visibility/split and five transformed targets consistent with the source label. Set each five-frame context to frame + [-2,-1,0,1,2] * max(1,floor(fps/30+.5)). All current labels support all three real target-aligned triplets; fail rather than clamp if a sample does not. Keep full-frame plus four 60% crops and the existing affine transform, 512x288 resolution and JPEG quality 95.

The preflight inventory requires 14,431 distinct source frames. Reuse 12,733 frames already cached by pilot04 and create 1,698 missing frames (8,490 five-view JPEGs) in a new output directory. Do not change the completed pilot04 cache. Verify all reused/new JPEGs decode to the expected shape and record their content hashes before freezing the new sample manifest. Existing image hashes are established by this preparation, not retroactively claimed as part of pilot04's original manifest.

For each clip, check the first, middle and last reusable source frame in all five views against a freshly transformed/JPEG-encoded original frame. Record the deterministic sample and exact byte agreement. This is a spot check of historical-cache source correspondence, not exhaustive pixel verification of all reused images. Validate every newly generated file against its generated bytes, keep source provenance and persist per-clip completion. A resume must verify the saved provenance and completed clip-file hashes; protect completed reports from overwrite.

The existing RandomSpatialView/SparseBallDataset can consume the five aligned paths unchanged because every current sample supports every output slot. No new augmentation or label interpolation is introduced. Test stride, target-slot identity, boundary rejection, duplicate source samples and target preservation. Render one train and one selection sample at 60 FPS plus one stride-one control, chosen by fixed clip/frame ordering, showing adjacent and spaced temporal context. Inspect these before any training launch.

No model is trained by this preparation step. Next training must separately freeze parameter scope, epoch count and detection-based acceptance against the actual deployment operating point, avoiding the previous selection of a threshold with inadequate proposal coverage. Expected work is several minutes of local CPU image preparation; geometric transforms and file decoding have no existing CUDA path. No download, paid compute or source-label change.
