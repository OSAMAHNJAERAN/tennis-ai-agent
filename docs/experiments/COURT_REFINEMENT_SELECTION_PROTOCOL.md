# Frozen court refinement selection controls

Evaluate all 32 preselected `selection` images from the completed `data/external/court_heatmap_pilot/manifest.json`, in manifest order. Freeze this protocol and the benchmark before inference. These groups were selected for an earlier heatmap training experiment and are present in the publisher training split. They are reused internal controls, not independent qualification data. ResNet training independence is unknown, and publisher annotations have known errors.

Copy the previous eight-image benchmark without changing detection, preprocessing, geometric calibration, line proposal, refinement, acceptance, scoring or rendering. Change only the evaluated partition, expected count, scope, source references and this protocol reference. Preserve the earlier benchmark and protocol byte for byte; record their SHA-256 values. Verify all dataset hashes and prior refinement implementation hashes before inference.

Compare raw, calibrated projection and refined predictions by the same fourteen keypoint identities and seven native pixel inclusive tolerance. All images are 1280 by 720. Wrong locations count FP plus FN; missing predictions count FN. Exclude only out-of-image annotations. Labels enter only after all prediction and acceptance decisions. Report available prediction coverage separately from mean/median error, and retain every image and regression without modifying annotations or thresholds.

Independently replay all per-point distances and counts, check unchanged labels and source hashes, and count every gained and lost correct match. Inspect all accepted corrections and all label regressions in their source-image context. Record which images were actually inspected. Rejected refinement must preserve initialization up to the known floating-point reference-scale round trip. A favorable aggregate must not conceal regressions or be presented as production readiness. No runtime behavior changes in this experiment.
