# Frozen court ridge filter ablations

The four-case missing-boundary diagnostic finds compatible LSD segments in every case, all discarded by photometric gates. Three clay baselines fail the low-saturation requirement; the Indian Wells near baseline fails similar-side brightness despite passing ridge contrast and whiteness. Source-context overlays were inspected for all four. Test the two factors separately and together, without adding boundary constraints or changing model predictions, geometric matching, optimization or acceptance thresholds.

Run all 64 saved evaluations (32 selection, eight external-source-check, 24 pilot) for each of three modes: no_white removes only the saturation condition; no_sides removes only the similar-neighbor-brightness condition; ridge_only removes both. All modes retain center brightness above 100 and at least ten grayscale levels above the brighter of both neighboring sides, existing ridge-center shifts, minimum segment length 30, support fraction 0.5 and every geometric threshold. Recompute the best ridge center under the selected conditions. The original algorithm and original proposal module remain byte-for-byte intact; replay original predictions against saved reports before comparison.

These are reused development data, including the motivating cases, not independent qualification. Keep labels, eligibility, native seven-pixel identity scoring and geometric failures unchanged. Compare per-case gains/losses and grouped metrics, including reduced precision or increased false court matches. Retain all mode results, do not select only the best subset. Evaluate all planned modes without adapting thresholds to intermediate scores. Record source, code and protocol hashes and every output. Render all usable cases. Inspect changed outcomes for the candidate selected for further work, including all losses, before any runtime consideration. Photometric relaxation can admit advertisements, net edges or clothing; image-line support does not by itself establish court accuracy.
