# Ball training expansion to sixty publisher clips

Declared 2026-09-14 before acquisition or model inference. Existing threshold and adaptation results show a remaining proposal/absence tradeoff. Expand the original forty publisher-training clips with the next twenty entries, indices 40:60, from RacketVision revision `85157ca21faa2abca96d837dd2b963738029bcc8`. Selection must be independent of model outcomes. Use the existing acquisition script with its 200 MB video limit and publisher SHA-256 checks. Preserve raw ball CSVs, source card and manifests; acquire no publisher test data.

Audit the three manifests together. Require identical pinned revision, no duplicate selected clips, membership in the original publisher training list, and no publisher validation match-ID overlap. Preserve the original every-fifth-distinct-training-match selection rule, extending it to sixty clips without moving any old match across the train/selection boundary. Do not treat publisher match IDs as verified independent broadcasts.

For each new clip, fully decode the video, verify frame count/dimensions and every CSV row, reject duplicate or out-of-range frame indices, validate explicit visibility and visible coordinates, and retain absent labels separately from missing annotations. Report duration, resolution, FPS, label prevalence, label density and temporal contexts available under native-FPS spacing. Sparse labels may supervise only their own target frame; neighboring frames remain context, never fabricated training targets.

Before training, render for each new clip its first visible and first absent label if present. Each panel includes the full labeled frame and three adjacent unmarked source crops. For absent labels use full-frame temporal context because no target location is defined. Include all such preselected examples, even ambiguous ones; preserve source labels and record review uncertainty rather than silently rewriting them. This is a deterministic coverage check, not exhaustive label certification.

Record all source, script and output hashes and completion state. No model training starts automatically from acquisition success. Use this audit to define a bounded follow-up training protocol, including whether confirmed label ambiguities require exclusion from training loss only and whether temporal spacing should match deployment. Original training/checkpoint manifests and every external validation score remain unchanged. Acquisition and decoding are preparation, not evidence of increased model accuracy or production readiness.
