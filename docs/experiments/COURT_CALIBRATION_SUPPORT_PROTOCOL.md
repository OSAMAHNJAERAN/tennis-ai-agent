# Frozen calibration support-ratio comparison

Compare the existing 0.75 minimum RANSAC inlier ratio with 5/7, holding the native three-pixel threshold and minimum six inliers unchanged. For fourteen available landmarks this changes the acceptance requirement from eleven to ten. With fewer available landmarks the ratio still applies to available correspondences; report those counts explicitly. This is one development ablation prompted by two rejected Madrid images, not an independently calibrated probability or a runtime default change.

Replay every raw ResNet prediction in the 125 remaining-pilot, 32 selection and eight external-source-check reports. Also replay the eighteen earlier camera/model controls: six UVY baseline/augmented predictions, six CalTennis baseline/augmented predictions and six broadcast heatmap predictions. Verify the default calibration decision and inlier count against saved results before comparing the alternate ratio. Keep all 183 evaluations, including invalid, missing and grossly misplaced predictions. These are reused cases and include repeated cameras/models.

Only newly accepted fits need new image processing. Project the canonical fourteen points using the alternate calibration, then run the unchanged combined no-white proposal and boundary-preserving refinement. Score labeled cases by unchanged identities and inclusive seven native pixels; missing predictions count FN and wrong localizations FP plus FN. Preserve original labels and outside-image exclusions. Report new coverage, gains and false localizations separately; do not interpret more accepted points as better accuracy. Existing accepted fits must remain identical.

Inspect every newly accepted case in source context, including unlabeled controls. Record line-support evidence, refinement decisions, source/code/protocol hashes and calibration matrices. A bad newly accepted fit is an explicit negative result, even if aggregate labeled recall increases. No threshold retuning, model loading, labeling changes or production calibration edits occur in this experiment.
