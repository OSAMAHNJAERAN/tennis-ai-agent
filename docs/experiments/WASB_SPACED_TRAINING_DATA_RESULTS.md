# FPS-aligned training inputs verified

Completed 2026-09-14. The existing sixty-clip WASB training cache now has a separately preserved FPS-aligned context manifest. This fixes the identified adjacent-training versus spaced-inference mismatch for 699 training labels and 150 internal-selection labels at 60 FPS. It prepares a controlled experiment; no accuracy improvement or new trained model follows from this cache alone.

All 2,999 labels retain their original clip/frame identity, source visibility, five transformed targets, order and train/selection membership. There are 2,399 training labels across 48 clips and 600 reserved labels across twelve clips. Every label supports three real target-aligned triplets. Context indices use frame plus [-2,-1,0,1,2] times max(1,floor(FPS/30+.5)), without boundary duplication. All stride-one view records remain exactly equal to pilot04, including their image paths.

Preparation reuses 12,733 source frames and writes crops for only 1,698 new frames (8,490 JPEG files). All 72,155 required cropped images decode to 512x288 BGR, and their hashes are frozen. Nine hundred deterministic source/crop spot checks reproduce the historical cached JPEG bytes exactly. These spot checks do not certify every historical cache pixel against source; that distinction is retained in the evidence. Every new crop is checked against generated bytes, and all video/CSV/manifest source hashes match. Preparation totals 808.45 seconds, primarily CPU decoding, hashing and geometry work; this is not GPU inference speed.

Independent review verifies every context filename/index/view, all hashes, label targets using a separate geometric calculation, unchanged sample order/split, and complete source-label coverage. The real existing SparseBallDataset is exercised on three fixed examples with twelve random seeds each, covering all output slots. All tensors are finite with expected shapes and binary target masks. Eleven focused context tests pass in 0.05 seconds.

All three preselected visual boards are inspected. `match176_000:9` shows a visible clay-court ball across adjacent 8/9/10 versus spaced 7/9/11 context. `match177_000:19` shows the wider player-motion interval with an identical central frame; its label is close to leg/shoe clutter and ball visibility is ambiguous. `match172_000:9` shows identical stride-one context and a moving light ball/streak above the net. These confirm temporal layout; source labels are unchanged and annotation quality is not newly certified.

The next bounded experiment is frozen in `WASB_SPACED_HEAD_PILOT05_PROTOCOL.md`: one head-only epoch from original weights, with the same targets, sampling, augmentation and optimization as pilot04. Evaluate at deployed threshold 0.20, compare both original and adjacent-head controls, and require proposal coverage preservation. Training has not been claimed by this preparation report. Overall production readiness remains **NO**.

Evidence:

- `artifacts/training/vision_upgrade/wasb_spaced_cache60/manifest.json`, SHA256 `b5e44444ac23f5cf2e347d0a59d407e0f98618a7616441fdeaf13c8cfa6e246a`.
- `artifacts/training/vision_upgrade/wasb_spaced_cache60/training_samples.json`, SHA256 `aaa288e8afc6bf0bc6ec4ca08134b99e49ac40440c6ff478eb2f78727fe31776`.
- `outputs/vision_upgrade_audit/wasb_spaced_training_data60/review.json`, `visual_review.json` and `context_00.jpg` through `context_02.jpg`.
- `scripts/data/prepare_wasb_spaced_training.py`, `scripts/evaluate/review_wasb_spaced_training_data.py`, and `scripts/evaluate/render_wasb_spaced_training_data.py`.
