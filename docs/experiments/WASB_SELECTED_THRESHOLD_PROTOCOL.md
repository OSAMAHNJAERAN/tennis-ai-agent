# Original WASB: frozen training-selected threshold transfer

Declared 2026-09-13 before external inference. The completed internal experiment selected 0.35 for the untouched original WASB checkpoint using 400 reserved training-match labels. That original-model result is distinct from the rejected spatial checkpoint. Test whether this already-selected operating point improves the original model on all eighteen existing development clips (900 labels). Do not train, choose another threshold, change the model, or select frames using external outcomes.

Use the same original checkpoint, full view plus four 60% corner crops, native-FPS spacing `max(1, floor(fps/30 + .5))`, real target-aligned triplets, averaged heatmaps, and peak-confidence ranking. Decode exactly 0.20 and the training-selected 0.35 from the same heatmaps. Component centers must be recomputed for each threshold. Preserve every proposal, selected point and source window. This is a sparse comparison without stationary, motion, residual or detour filters.

Verify the complete training-selection report and reviewer, checkpoint identity, current inference hashes, unchanged source manifests/files and all 900 original CSV labels. Training match IDs must be disjoint from the evaluated publisher validation IDs. Require every recomputed 0.20 selected point and missing state to match the existing spaced raw stream within 0.0001 native pixels. On the first label of each clip, also compare the control proposals and windows with the existing inference function. Persist each completed clip and validate provenance before resuming; never restart a live process.

Independently score all paired rows with the inclusive four-pixel tolerance in 512x288 reference space, counting wrong visible localization as FP plus FN. Report per-clip counts, pooled precision/recall/F1, absence errors, proposal coverage, and changed outcomes. Keep labels unchanged after visual review. These repeatedly examined development clips cannot establish independent production reliability.

Advance to a separate continuous comparison only if candidate pooled F1 exceeds the paired raw control and neither precision nor recall decreases. Otherwise reject promotion at this gate, retaining all outcomes. The known final filtered 0.20 result is contextual evidence only; comparing it directly against sparse raw 0.35 would mix processing stages. Even a sparse pass requires all-frame filters, output video review and independent qualification before runtime changes.

Render at most the first two examples in each gained/lost visible/absence category, sorted by clip/frame, plus any changed visible localization counted on opposite sides of the tolerance boundary. Save source, code, report and image hashes. Expected inference is approximately ten minutes on the existing RTX 4050 based on prior sparse timings, with no paid resources and no new checkpoint downloads. Actual time and GPU information must be recorded rather than inferred from this estimate.
